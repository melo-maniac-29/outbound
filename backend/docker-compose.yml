version: '3.8'

services:
  outbound_agent:
    build: .
    container_name: outbound_agent
    env_file:
      - .env
    volumes:
      - .:/app
    extra_hosts:
      - "host.docker.internal:host-gateway"
